Setup Git For first

Steps are following.
1)git init #it help you to initialize project as git project in local repositry
2)git remote add origin "your origin" # remote like github , gitlab , bitbucket

Note above commands are once in life command

setp daily to be use

1) git add -A
2) git commit -m "Mssage for add"
3) git push origin [branch]
4) git pulll origin [branch]
5) git branch 
6) git checkout [branch name]